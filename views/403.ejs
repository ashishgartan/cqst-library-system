<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>
  <title>Access Restricted | CQST Library</title>

  <body class="min-h-screen bg-slate-50 flex flex-col items-center justify-center p-6 font-sans">
    
    <div class="text-center space-y-8 max-w-md">
      
      <div class="relative inline-block">
        <div class="absolute inset-0 bg-red-100 blur-2xl rounded-full opacity-60"></div>
        <div
          class="relative w-24 h-24 bg-white border border-red-100 rounded-[2rem] flex items-center justify-center mx-auto shadow-xl"
        >
          <i data-lucide="shield-off" class="w-12 h-12 text-red-600"></i>
        </div>
      </div>

      <div class="space-y-3">
        <h1
          class="text-7xl font-black text-slate-900 tracking-tighter"
        >
          403
        </h1>
        <h2 class="text-xl font-extrabold text-slate-800 uppercase tracking-tight">
          Unauthorized Access
        </h2>
        <p class="text-slate-500 font-medium text-sm leading-relaxed px-4">
          This secure protocol is reserved for <span class="text-slate-900 font-bold">Librarians and Staff</span> only. If you believe this is an error, please contact the administration.
        </p>
      </div>

      <div class="pt-4 flex flex-col gap-3 max-w-[240px] mx-auto">
        <a
          href="/"
          class="bg-red-600 hover:bg-red-700 text-white font-black py-4 px-8 rounded-2xl transition-all shadow-lg shadow-red-200 flex items-center justify-center gap-2 active:scale-95"
        >
          <i data-lucide="home" class="w-4 h-4"></i>
          Return Home
        </a>
        
        <button
          onclick="history.back()"
          class="text-slate-400 hover:text-slate-900 text-xs font-black uppercase tracking-widest transition-colors py-2"
        >
          Go Back
        </button>
      </div>
    </div>

    <div
      class="absolute bottom-8 text-slate-300 text-[10px] font-black tracking-[0.3em] uppercase"
    >
      CQST Security Infrastructure
    </div>

    <script>
      lucide.createIcons();
    </script>
  </body>
</html>