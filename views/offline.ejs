<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Connection Lost | CQST Library</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap");
      body {
        font-family: "Plus Jakarta Sans", sans-serif;
        background-color: #f8fafc;
        background-image: radial-gradient(#e2e8f0 1px, transparent 1px);
        background-size: 32px 32px;
      }
      .error-card {
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(12px);
        border: 1px solid #ffffff;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.05);
      }
    </style>
  </head>
  <body
    class="text-slate-700 min-h-screen flex items-center justify-center p-6"
  >
    <div class="max-w-md w-full text-center">
      <div
        class="error-card rounded-[3rem] p-10 md:p-14 relative overflow-hidden"
      >
        <div class="relative inline-flex mb-8">
          <div
            class="absolute inset-0 rounded-xl bg-red-100 animate-ping opacity-75"
          ></div>
          <div
            class="relative inline-flex items-center justify-center w-24 h-24 rounded-xl bg-red-50 text-red-600 border border-red-100 shadow-inner"
          >
            <i data-lucide="wifi-off" class="w-10 h-10"></i>
          </div>
        </div>

        <h1 class="text-4xl font-black text-slate-900 tracking-tight mb-4">
          System Offline
        </h1>

        <p class="text-slate-500 text-sm font-medium leading-relaxed mb-10">
          The CQST Library management system requires an active connection.
          Please check your network and try again.
        </p>

        <button
          onclick="window.location.reload()"
          class="w-full bg-slate-900 hover:bg-red-600 text-white font-black py-4 rounded-xl transition-all shadow-xl shadow-slate-200 flex items-center justify-center gap-3 uppercase tracking-widest text-xs active:scale-95"
        >
          <i data-lucide="refresh-cw" class="w-4 h-4"></i>
          Retry Connection
        </button>

        <div class="mt-8 pt-8 border-t border-slate-100">
          <p
            class="text-[10px] font-black text-slate-300 uppercase tracking-[0.3em]"
          >
            Network Status: Disconnected
          </p>
        </div>
      </div>

      <p
        class="mt-8 text-slate-400 text-[10px] font-bold uppercase tracking-widest"
      >
        CQST Library Service &copy; 2026
      </p>
    </div>

    <script>
      lucide.createIcons();

      // Auto-refresh when back online
      window.addEventListener("online", () => {
        window.location.reload();
      });
    </script>
  </body>
</html>
