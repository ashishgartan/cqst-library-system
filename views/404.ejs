<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - Page Missing | CQST Library</title>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="flex flex-col min-h-screen bg-slate-50">
    <%- include('partials/header') %>
    

    <main
      class="flex-grow flex items-center justify-center relative overflow-hidden px-4 py-20"
    >
      <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-red-100/50 blur-[100px] rounded-xl"
      ></div>

      <div class="relative text-center">
        <h1
          class="text-[10rem] md:text-[12rem] font-black text-slate-900 leading-none tracking-tighter opacity-10 select-none absolute -top-24 left-1/2 -translate-x-1/2"
        >
          404
        </h1>

        <div class="relative">
          <div
            class="inline-block bg-slate-900 text-white text-[10px] font-black px-3 py-1 uppercase tracking-[0.2em] mb-6"
          >
            Error: Missing Volume
          </div>

          <h2
            class="text-3xl font-black text-slate-900 uppercase tracking-tight"
          >
            Page Not <span class="text-red-600">Found</span>
          </h2>

          <p
            class="mt-4 text-slate-500 max-w-sm mx-auto leading-relaxed font-medium"
          >
            The resource you are looking for has been moved, archived, or never
            existed in our collection.
          </p>

          <div
            class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4"
          >
            <a
              href="/"
              class="inline-flex items-center gap-2 px-8 py-4 font-black text-white bg-red-600 hover:bg-red-700 rounded-xl transition-all shadow-lg shadow-red-200 active:scale-95"
            >
              <i data-lucide="arrow-left" class="w-4 h-4"></i>
              RETURN TO DASHBOARD
            </a>

            <button
              onclick="history.back()"
              class="px-8 py-4 font-black text-slate-400 hover:text-slate-900 transition-colors uppercase text-[10px] tracking-widest"
            >
              Go Back
            </button>
          </div>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>

    <script>
      lucide.createIcons();
    </script>
  </body>
</html>
